{
  "name": "hydra-ui-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pretest": "node -e \"require('fs').rmSync(require('path').resolve(__dirname,'..','tests','fixtures','sample_session','hydra'),{recursive:true,force:true})\"",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.7.0"
  }
}
